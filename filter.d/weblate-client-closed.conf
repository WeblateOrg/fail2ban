[Definition]

failregex = ^<ADDR> \S+ \- \[\] \"[A-Z]+ \S+ \S+" 499 .+$

ignoreregex = "(POST|GET) (/(js|translate|static|avatar|widgets?|media)/|.*\.(css|js|svg|png) )
              "GET /screenshot/[0-9]+/view/(?:\?[^\s"]*)? HTTP/
              "Site24x7"$
